// Input principal amount, input rate of interest, input number of years, Then Calculate simple interest.

let amount = 100;
let interest = 50;
let time = 1;
let totalInterest = time*interest*amount;

console.log("Total amount to pay after interst =", (totalInterest)/100 + amount);